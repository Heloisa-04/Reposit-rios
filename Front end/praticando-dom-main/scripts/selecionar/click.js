// Exibe o elemento clicado no console
document.body.addEventListener("click", event => { // Checar o evento que foi especifiado no caso o click
    console.log("Click em:", event.target); // Imprime uma mensagem no console
  });
  

  