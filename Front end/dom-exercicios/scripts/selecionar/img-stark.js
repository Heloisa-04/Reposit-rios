const imgStark= document.querySelectorAll('img[src^="./img/stark"]');
//variavel      seleciona os elementos css  -- o caminho do elemento css a ser selecionado
                                                // é especificado que queremos o src

//Imprime a mensagem no console
console.log("Imagens Stark:", imgStark);